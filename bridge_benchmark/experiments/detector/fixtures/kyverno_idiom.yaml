apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata: {name: replica-by-tier}
spec:
  validation:
    cel:
      expressions:
        - expression: "object.spec.replicas <= limits[object.metadata.labels.tier]"
